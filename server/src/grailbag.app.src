{application, grailbag, [
  {description, "artifact repository"},
  {vsn, "0.0.0"},
  {registered, []},
  {applications, [kernel, stdlib, crypto]},
  {modules, []},
  {mod, {grailbag_app, []}},
  {env, [
    %{data_dir, "..."}            % `file:filename()'
    {log_handlers, []},           % list of `{Module, Args}'
    {listen, [{localhost, 3255}]} % list of `{Address, Port}'
  ]}
]}.
